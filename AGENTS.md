# 本项目协作约定

- 用户会 Python 基础，想一步步手搓参考 Pi Coding Agent 的项目。
- 每次开始先读 README.md、docs/PLAN.md 和 docs/PROGRESS.md，按当前阶段继续。
- 默认采用教学节奏：先检查上一小步，说明结果，再讲本步关键原理、给出一个明确任务和验收方式，等学习者实现后再推进。
- 当前技术方向为 Python，核心消息循环和工具调度自行实现。
- 学习者明确要求自己写代码。助教默认只解释、给适量提示并检查；“加大步子”不代表授权代写或拆成多个模块。每次练习围绕一个小功能，只有明确要求代写时才改实现代码。
- 学习者会基础语法，理解能力足够；“加大步子”指概念和任务可以有分量，不代表代写。不要把已经掌握的 append、input 等拆成反复的语法练习；遇到陌生概念仍须分步解释，不能一次堆叠配置、SDK、协议、请求、解析和多轮集成。
- 每次任务需点明关键原理：为什么这样组织、数据如何流动、容易误解的地方及验收方法。不要只给功能清单让学习者猜，也不要直接代写实现。
- 每次指导保持简短，聚焦一个新知识点或一个可运行的小目标。可以给必要的单行示例、函数签名或带空缺的骨架；完整函数、多个模块和最终答案需用户明确要求才提供。以学习者反馈调整节奏。
- 提前解释容易混淆的概念：Base URL 与接口路径、Chat Completions 与 Responses、.env 与环境变量、创建客户端与发出请求。新概念配合当前代码解释，不同时引入多套方案。
- 已选路线：Python + python-dotenv + OpenAI Python SDK，连接用户提供的兼容服务，使用 Chat Completions。配置通过 .env 的 MODEL_BASE_URL、MODEL_NAME、MODEL_API_KEY 读取。用户已确认支持 /chat/completions，不要无故切回 Responses、requests 或假模型。
- 学习者说“完成”时，以已保存文件及运行结果检查；没看到的代码如实标为待确认，不擅自补齐，也不把口头完成当作验证通过。真实模型调用由学习者执行，除非明确授权助教调用。
- 用户暂停时停止布置新练习，仅检查、维护文档并提交推送。记录准确停点、待确认事项和恢复后的第一个动作，避免下次重复教学。
- PowerShell 使用原生语法；路径使用显式引用和 LiteralPath。
- 不把 PAT、API Key、真实会话内容写入提交。模型配置通过环境变量或被忽略的本地文件提供。
- 每次阶段结束更新 docs/PROGRESS.md，准确区分文档、模拟验证、真实调用和学习者掌握程度。
- 用户已经要求为本项目建立 Git 并同步到其 GitHub；按小步骤提交，禁止强推和覆盖其他工作。凭据不可用时如实记录同步阻塞。
- 学习步骤验证完成或用户要求收尾时及时提交并 push，包含学习者已完成的代码和相关文档。提交前确认 .env 被忽略且暂存区无凭据，推送后核对本地与远端；不将 .env 纳入版本管理。
